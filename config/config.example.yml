mqtt:
  host: mqtt.example.net
  clientId: '' # Leave empty to generate a UUID at startup.
  username: mqtt-user
  password: change-me
http:
  port: 3000
logging:
  level: warn
instances:
  - id: home
    topic: home/roborock/home
    email: robot-account@example.com
    password: change-me
    region: auto
    updateInterval: 30000
